#include<bits/stdc++.h>
using namespace std;

int main()
{
    int year;
    cin>>year;
    if(year > 1984)
    {
        if(year%4 == 0)
        {
            int days = 366;
            days-=29;
            int itr = 0;
            while(days/30 != 0|| days/31 != 0)
            {
                itr++;
                days = days
            }
        } 
    }
    return 0;
}